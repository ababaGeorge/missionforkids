# LAND & DEPLOY REPORT — PR #5

```
PR:           #5 — feat: 真帳號系統 — email 真帳號取代全匿名登入（Plan A/B/C/D）
Branch:       feat/real-user-accounts → main
Merged:       2026-06-02T06:37:57Z (merge commit)
Merge SHA:    93a257594e8ca9f3099a97d5452a956fe9fe0110
Merge path:   direct (gh pr merge --merge --delete-branch)
First run:    yes (land-and-deploy first use on this project)
Project type: Expo/RN + Firebase mobile app (no web auto-deploy)

Timing:
  CI wait:    none (Vercel preview already green)
  Deploy:     n/a — backend deployed manually last session
  Canary:     n/a — no live web product (iOS/EAS build is the product)

Tests (run fresh pre-merge):
  RN/root jest:    13/13 pass
  functions jest:  51/51 pass (firebase emulators)
  resend warn:     expected, non-fatal (codex #1, email send failure path)

Prod backend (verified live via Firebase MCP):
  Functions:   9/9 expected
    acceptFamilyInvite, analyzePhoto, autoCompleteDeliveredOrders,
    bootstrapParentAccount, createFamilyInvite, grantPoints,
    onRewardOrderCancelledOrRejected, onRewardOrderCreated,
    onTaskInstanceApproved
    (redeemInvite + bootstrapDevSession gone, as designed)
  Rules:       deployed (inviteCodes removed)

Reviews:
  Plan D codex review: PASS (last session, gate PASS)

CI:           PASSED (Vercel preview)
Deploy:       N/A (backend already live; mobile app has no merge-triggered deploy)
Verification: BACKEND VERIFIED (9 functions live + rules deployed)

VERDICT: MERGED — real-user-accounts (Plan A/B/C/D) fully landed on main.
         Backend already live and verified. No further deploy step needed.
```

## Notes
- The merge to `main` is the only state change this run. Firebase functions + rules
  were deployed to prod in the prior session (2026-06-02 ~03:25) and re-verified here.
- Mobile app users get the iOS/EAS build, not a web URL — there is no canary target.
  The passing Vercel check is an Expo-web preview, not the shipped product.
- Open follow-ups carried forward (non-blocking): codex #1 (email resend path),
  codex #3 (cleanup-anon scope guard), 2 residual rejected-order docs (noise),
  and 🔴 Resend own sending domain before real users.
