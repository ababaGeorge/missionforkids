# Claude Cowork Desktop — Run 02 回測啟動 Prompt（改善輪 R1 修復驗證）

> 把「---」以下整段貼給 Claude Cowork Desktop。前提：分支 `fix/cowork-round1` 已 checkout、Metro 跑的是這個分支的 code。

---

你是 Mission for Kids（Expo 54 + RN + Firebase）的**回測代理**。上一輪（Run 01，見 `docs/testing/results/2026-07-06-test-run-01.md`）發現的問題已在分支 `fix/cowork-round1` 修復，你的任務：**逐項驗證修復生效**＋**補測上輪因環境限制沒測到的項目**。

## 專案位置
`/Users/ababa_george/Desktop/missionforkids project/missionforkids`

## ⚠️ 開測前先修環境（Run 01 最大教訓）
1. **關硬體鍵盤**：Simulator 選單 I/O → Keyboard → 取消勾選 Connect Hardware Keyboard；並確認模擬器內鍵盤為英文。開測前先在登入頁 email 欄實際打幾個字驗證輸入可用——**輸入不通就停下回報，不要帶病開測**。
2. 模擬器／App／Metro 還原步驟同 `docs/testing/claude-cowork-kickoff.md` 的「環境還原」節（帳號、快速登入鈕、護欄也全部沿用該文件）。
3. 截圖若 `save_to_disk` 仍失效，照 Run 01 慣例以文字詳述畫面替代。

## 第一部分：修復驗證（P1–P9，逐項判 ✅修復/❌未修復/⚠️部分）
| # | 驗什麼 | 怎麼驗 |
|---|---|---|
| P1 | 「我拿到了」後按鈕死鎖 | 完整走 兌換→核准→交付→我拿到了 → 底部「回到主頁」「回到任務」**要能點**；點數不重複扣 |
| P2 | 任務指派三症狀 | 家長建任務指派小安 → 卡片**立即**顯示「小安・0/1」（不用重登）；開編輯 modal「指派給」**有預填**；同一任務編輯儲存 2 次 → 小孩端**只有一份**、卡片仍 0/1 |
| P3 | 家長通知漏最新 | 小孩提交任務＋兌換 → 家長通知頁**立刻**出現這兩筆在最上面 |
| P4 | 假統計 | 小孩「我的」頁：連續天數是真數字（今天完成核准後應 ≥1）、「讀書蟲」消失、改為里程碑徽章 |
| P5 | 商城鎖說明 | 有進行中訂單時，灰卡上出現「先完成上一個兌換喔」；無訂單且買不起顯示「還差 N」 |
| P6 | 退點延遲困惑 | 取消訂單 → 出現「已取消／星星會在幾秒內退回你的錢包 ✨」→ 按「好」關頁 → 數秒後餘額退回 |
| P7 | 登入回饋 | 空欄位→登入鈕不可按（變淡）；錯密碼→密碼欄下方**中文**紅字（不再是英文彈窗）；按 dev 快速鈕→舊紅字要消失 |
| P8 | 審核 sheet 餘額 | **本輪看到 fallback 行為即預期**（CF 尚未部署，新訂單還沒有快照）→ 只驗 sheet 正常顯示不壞；真正快照驗證在部署後補 |
| P9 | 對受邀小孩發點 | 併入第二部分 E8/E9 流程驗 |

## 第二部分：補測（Run 01 未測項，環境修好後應可測）
- **E8/E9 Email 邀請全流程**＋對受邀綁定的小孩發點（= Run 01 重點②，程式碼分析判定應該正常，用實測蓋章）
- **E3/E4/E5**：扣點、clamp（扣超過餘額）、冪等
- **④**：快速連點「建立任務」「建立禮物」送出鈕 → 不應長出重複
- **⑧**：once 任務反覆編輯 → 截止日不漂移；**⑨**：通知累積 >20 筆 → 最新的不消失（P3 修後應全列）
- **C3/C4**：審核退回（含留言）、退 3 次變 missed
- **A9-A11 註冊**、**B1/D1 自訂名稱建任務/禮物**（自由文字輸入現在應該可用）

## 產出
寫到 `docs/testing/results/2026-07-08-test-run-02.md`：
1. 第一部分逐項 ✅/❌/⚠️ ＋實際觀察；2. 第二部分逐項結果＋新缺陷（沿用主計畫第 6 節模板）；3. 整體結論：R1 修復是否全數生效、可否 merge。

## 護欄（同 Run 01，另加）
- 你在**分支**上測試：不要動 git（不 commit/switch/pull），只讀寫 `docs/testing/results/`。
- 其餘護欄照 `claude-cowork-kickoff.md`：只動 dev-family-seed、不碰 Firebase 寫入/部署、崩潰記錄後續行、不確定標「待確認」。
