# Run 03 部署後補測報告

> 日期：2026-07-10 ｜ 基準：main @ 41647d4（PR #10 merge、functions/rules/storage 已部署 prod）｜ 環境：iPhone 17 Pro（A, B4436202, 家長）＋ iPhone 17（B, C6986BCA, Kid3）雙模擬器、Metro dev client（--clear 重啟）連 prod、Maestro 2.6.0 座標驅動＋截圖＋Firebase MCP 後端讀值
> 範圍：`2026-07-09-test-run-r2-sim.md` 末節列出的部署後補測清單。

## 結果：6 測項——5 通過、1 標註不重現（R2-06）、R2-25/26/27 評估後建議延後

| # | 驗證項 | 結果 | 關鍵證據 |
|---|---|---|---|
| C1 | CX-1 rules 狀態機 prod 生效 | ✅ | MCP 重讀 prod firestore rules：taskInstances/rewardOrders 流轉矩陣＋`changedKeys()` 白名單＋create 禁預埋快照全在；storage rules 12MB（R2-12）生效 |
| C2 | R2-13 clamp 提示三分支 | ✅ | ①對小安（餘額38）扣 999 → Alert「-38 ★ → 小安 (已依餘額調整)」，錢包 38→0（MCP）②對小宇（餘額0）扣 10 → Alert「小宇 的餘額已是 0，這次沒有扣點」，錢包不動 ③給小安 +38 → Alert「+38 ★ → 小安」（無調整註記），錢包回 38 |
| C3 | R2-29 removed 成員重邀 reactivate | ✅ | 家長移除 Kid3（membership→removed，MCP 證實）→ 新邀請 `2zvBzjKTJ5QOa0LUioBj` → Kid3 帶既有帳號接受 → membership 回 active、**joinedAt 保留原值**（落地決策）、**錢包 10 點原封不動**（updateTime 未變）、invite→accepted、家長清單即時回歸 |
| C4 | R2-05 註冊恢復（ensureAuthUser）兩分支 | ✅ | prod 實測：①同 email＋錯密碼 → Alert「無法加入／這個 Email 已經註冊過但密碼不符，請輸入原本設定的密碼」②同 email＋正確密碼 → 自動登入取回 session、補跑 acceptFamilyInvite 成功進入（C3 全鏈即走此路徑） |
| C5 | R2-24 家長完整註冊 | ✅ | dev-parent2@mfk.test 全新註冊 → Auth uid `bmuhpx…1853`、`users/`（roleType=parent）、`families/xBNBwJRowZI8Jkj2lMkA`（TestFamily2, graceDays=2）、`familyMemberships/{uid}_{familyId}`（parent/active）三件套落地＋直進家長首頁。**附帶驗證失敗原子性**：中途一次密碼空欄送出 →「註冊失敗，請再試一次」且 Auth 零殘留（無孤兒帳號） |
| C6 | FIX-A E2E 補步（建單即 approve 仍扣款） | ✅ | `functions/scripts/core-loop-e2e.cjs` 新增 section 17（17.1–17.3）：pending 訂單立刻 approve → 錢包 80→30 照扣、快照欄位寫入、訂單維持 approved、`pointTransactions` ledger delta=-50。**81/81 兩次全綠**（原 78 步零退化），commit `9fa2f21` |
| — | R2-06 逃生出口（8 秒 stuck UI） | ⚠️ 未實測 | 需「已登入但 profile 未建成」持續 8 秒的狀態，prod 模擬器無法安全精準重現（要在 bootstrap CF 在途時切斷網路）。維持 emulator E2E 覆蓋；真要實測需刻意斷網重現，列 Run 04 選項 |

### 附帶驗證（非清單項）
- **A17 rules 隔離**：Kid3 被移除期間，client 端 wallet/tasks 讀取即刻 permission-denied（dev toast 可見）＝removed 成員被 rules 真實擋下。
- 成員移除後家長端清單**即時**消失（對照 R3-1 任務清單的非即時問題，成員清單這條鏈是即時的）。
- R2-19 再驗：Kid3 我的頁顯示「加入 1 天」（joinedAt 保留 07-08 的正確天數計算）。

## R2-25 / 26 / 27 成本評估（建議延後，未執行）
- **R2-25（退回 3 次→missed）**：UI 全跑需「小孩拍照提交＋家長退回」×3 輪；計畫文件本建議用 seed 腳本把 `submissionCount` 造到 2 再跑最後一輪，但 agent 對 prod 的 admin 直寫被擋（走使用者授權才可）。建議：Run 04 以 seed 腳本＋單輪 UI 收掉，或交下輪 Cowork。
- **R2-26（通知 >20）**：需 21+ 筆真實通知事件；手動 UI 製造成本極高，seed 直寫同上受限。建議：寫 seed 腳本後再測。
- **R2-27（低優先回歸池）**：計畫明標「視成本挑做、與其他輪合併」。本輪未挑；D1–D4 禮物 CRUD（Run 02 被測試工具軟鍵盤卡住的懸案）本輪在 B 機同樣遇到軟鍵盤 flakiness，建議下次在 A 機（鍵盤穩定）挑測。

## 測試殘留（prod）
1. **TestFamily2 全套**（R2-24 產物）：Auth `dev-parent2@mfk.test`（uid `bmuhpxfNiFQMPubvLnOovHM41853`）＋ `families/xBNBwJRowZI8Jkj2lMkA` ＋ users/membership doc。留作「第二家庭」fixture 或清理——**併入 R2-28 授權清單**。
2. 邀請 doc：`familyInvites/2zvBzjKTJ5QOa0LUioBj`（accepted）。
3. 小安錢包過程 38→0→38（多 2 筆 pointTransactions：clamp 扣點與回補）；小宇多 1 筆 delta=0 冪等標記。
4. 模擬器 B 已登回 Kid3；模擬器 A 家長；Metro 背景執行中。

## 測試環境觀察（非 App 缺陷，寫給下次跑模擬器實測的人）
1. **dev client 兩次閃退回主畫面**（A、B 各一次，皆在 Maestro tap 後）：Metro 無任何 JS fatal，`simctl launch`/deep link 重開即回到原頁面續測。疑 dev client＋Maestro XCTest driver 偶發，非 App bug；再發可改 release build 驗證。
2. **模擬器 B 軟鍵盤間歇不出**：`inputText` 偶爾無效（密碼欄尤甚），且 iOS「要儲存密碼嗎」prompt 會插隊。繞法：`simctl pbcopy` ＋ 長按欄位 → 原生「貼上」選單（Maestro 對原生選單文字可命中）；或送出前截圖確認欄位有內容。
3. **LogBox 錯誤 toast 會攔截 tab bar 點擊**：先點 toast 的 ✕ 再點 tab。
4. secure 欄位（密碼）未聚焦時不顯示點點，「看起來空的」不可信——用送出結果或 Auth 查詢判定。

## Run 04 候選
- R2-25 / R2-26（先寫 seed 腳本）、R2-27 挑測（A 機）、R2-06 斷網重現（可選）、FIX-A prod 手動 race（可選——E2E 已覆蓋，prod 冷啟動窗口若要親驗再排）。
