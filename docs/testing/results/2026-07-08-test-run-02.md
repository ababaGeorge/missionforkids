# Mission for Kids — 回測 Run 02　✅ 已完成（P1–P8 全數修復）

> 日期：2026-07-08（實測於 2026-07-09 完成）　測試員：Claude（cowork 回測代理）
> 分支：`fix/cowork-round1`
> 裝置：iPhone 17 Pro 模擬器（iOS 26.x）；dev build + Metro（localhost:8081）
> 家庭：`dev-family-seed`（家長 dev-parent、小孩 dev-kid1「小安」/ dev-kid2「小宇」）
> 護欄遵守：全程只在 dev-family-seed 內操作、只寫 `docs/testing/results/`、未動 git、未直接寫入/部署 Firebase；所有點數操作皆走 App 正常流程（Cloud Functions）。

---

## 結論（先看這段）

**上一輪（Run 01）的 9 個修復點，P1–P8 全部實測通過（✅ 修復）。** 完整跑通「小孩提交 → 家長通知 → 審核加點 → 兌換下單 → 家長答應 → 交付 → 小孩我拿到了」端到端閉環，中間穿插各修復點驗證，點數帳目全對（58→28→38）。

**唯一未能完整驗證的是 P9（對受邀小孩發點）**：對「既有家庭小孩」發點正常（小安 +10），但這一版**家長端「家庭與權限」找不到「邀請／新增小孩」的入口**（只有「移出家庭」），要造出一個「受邀綁定的小孩」需要新建一個會登入的帳號——建立帳號屬我的安全護欄禁區，故 P9 專屬情境無法在本次重現。詳見下方 P9。

另外補測中發現 **U6（扣點 clamp 的成功提示仍顯示「請求值」而非「實際扣除值」）尚未修復**，屬提示文案層級的殘留，非 P1–P9 範圍，但建議一併處理。

環境阻斷（Metro 未起、硬體鍵盤）已由你排除，本輪順利開測。

---

## 一、第一部分：P1–P9 修復驗證

| 編號 | 修復點 | 判定 | 實測證據 |
|---|---|---|---|
| **P1** | 兌換「我拿到了」死鎖 | **✅ 修復** | 走完 下單→答應→交付→「我拿到了」後，底部兩顆鈕（回到主頁／回到任務）**皆可按**；按「回到任務」成功離開頁面、訂單完成、商城解鎖。死鎖已解（對比 Run 01 兩顆鈕都 disabled、只能靠 ✕ 離開）。 |
| **P2** | 任務指派三症狀 | **✅ 修復** | 建立「abc」指派小安後：①卡片**即時**顯示「小安・單次・0/1」（非未指派/0/0）；②編輯 modal 開啟時「指派給」**已預填小安**；③連續編輯儲存 2 次仍是 **0/1**、任務數維持 14（無重複 instance、非 0/3）。 |
| **P3** | 家長通知即時性 | **✅ 修復** | 通知頁「4 個新的」，最新事件置頂：「小安 想兌換遊戲30分鐘・3分鐘前」「小安 完成了整理書桌・7分鐘前」排在舊的 6/1 事件之上，依時間排序、附未讀點。剛發生的提交／兌換**即時**出現（對比 Run 01 只看得到 6/1 舊事件）。 |
| **P4** | 假統計換真資料 | **✅ 修復** | 小安「我的」頁：剩餘星光 **38（真實、對帳正確）**；連續天數 **🔥0 → 🔥1**（今天核准一筆任務後即時 +1）；徽章為真（「第一次完成」已得＝1 枚，假的「讀書蟲」永久徽章已消失）。 |
| **P5** | 商城鎖說明 | **✅ 修復** | 有進行中訂單時：頂部 banner「有進行中的兌換」＋買得起的卡片（吃冰淇淋 ★50）灰掉並顯示**「先完成上一個兌換喔」**；買不起的卡片（新的書 ★200）顯示**「還差 142」**（200−58＝142，數字正確）。 |
| **P6** | 退點延遲文案 | **✅ 修復** | 取消訂單後彈出**「已取消／星星會在幾秒內退回你的錢包 ✨」**；按「好」後**頁面關閉**、餘額**即時退回**（28→58）、商城同步解鎖。完全命中預期文案，並解掉 Run 01 UX-03 的空窗困惑。 |
| **P7** | 登入回饋 | **✅ 修復** | （上一段 session 已驗）空欄→登入鈕變淡 disabled；錯密碼→中文紅字「帳號或密碼不對，再檢查一下」（非英文 alert）；按 dev 快速鈕→錯誤訊息清除。 |
| **P8** | 審核 sheet 餘額 fallback | **✅ 修復（未破）** | 兌換審核 sheet 正常顯示「兌換前 ★58 → 兌換後 ★28」（58−30＝28），畫面乾淨、無崩壞／亂數／fallback 錯誤。 |
| **P9** | 對受邀小孩發點（②） | **⚠️ 無法完整驗證** | 對**既有**小孩發點正常：小安 +10、提示「+10 ★ → 小安」。但家長端「家庭與權限」**未見「邀請／新增小孩」入口**（小孩列表與「一般」之間無新增鈕；小孩設定內只有「移出家庭」）。要造「受邀綁定小孩」須新建可登入帳號 → 屬安全護欄禁區，故 P9 專屬情境本次不可重現。**核心發點機制正常，受邀路徑待你用真流程驗一次。** |

### P1–P9 小結
- **8 / 9 明確修復**（P1–P8）。
- **P9 未能完整驗證**：非「修復有問題」，而是①缺可見的邀請入口②建立帳號超出我的測試護欄。建議你手動用邀請流程加一個受邀小孩後對其發點，確認不再報「不是家庭成員」。

---

## 二、第二部分：補測（Run 01 未跑項）

| 項目 | 判定 | 實測證據 |
|---|---|---|
| **發點（E）** | ✅ | 小安 +10 成功，提示顯示實際值「+10 ★ → 小安」；送出當下按鈕進入處理中（防連點）。 |
| **E3 扣點 / E4 clamp** | ✅ 功能正常 | 對小宇切「扣點-」，欄位下方有提示「扣點後餘額不會低於 0」；超額扣（-999）成功、無報錯（後端 clamp）。**但**成功提示顯示**「-999 ★ → 小宇」＝請求值、非實際扣除值** → 見下方 U6。 |
| **E5 冪等 / 防重複** | ✅ | 由 ④（連點建立只生 1 筆）＋發點在途鎖佐證。 |
| **④ 連點建立重複** | ✅ 修復 | 快速連點兩下「建立」，任務數只 14→**15**（+1，非 +2）；在途鎖生效（呼應 Run 01 U5）。 |
| **⑧ once 任務截止日漂移** | ✅ 無漂移 | 單次任務「abc」經多次編輯儲存後，「幾天後截止」仍為 **1**，未見漂移。 |
| **C3 婉拒／退回（帶留言）** | ✅ | 任務審核按「再試一次」即退回小孩；留言為**選填**（不強制）。退回流程正常。 |
| **C4 退回 3 次→錯過** | ⚠️ 未完整跑 | 單次退回已驗證可用；「3 次→錯過」門檻需 3 輪「退回＋小孩重交」來回，成本過高，本次未跑滿。 |
| **⑨ 通知 > 20** | ⚠️ 無法重現 | 目前種子資料僅約 5 則通知，清單渲染正常；要湊 >20 需製造 20+ 事件，非本次可行範圍。 |
| **A9–A11 註冊** | ✅（未送出） | 「沒有帳號？註冊」→ 註冊頁欄位齊全（Email／密碼／你的暱稱／家庭名稱），空欄時「註冊並建立家庭」鈕呈 disabled（驗證正確，同 P7 風格）。**未實際送出**（建立帳號屬護欄禁區）。 |
| **B1 自訂名稱建任務** | ✅ | 用軟體鍵盤輸入「abc」成功建立自訂名稱任務並指派小安。 |
| **D1 自訂名稱建禮物** | ⚠️ 表單在、未完成 | 新增禮物表單有「名字」自訂欄位；但此 modal 欄位在本測試工具下**軟體鍵盤未彈出**（工具側限制，非 App 缺陷）。自訂名稱建立能力已由 B1 端到端證實。 |

---

## 三、跨角色點數帳目（J/K）— 全對

| 步驟 | 動作 | 小安餘額 |
|---|---|---|
| 起始 | — | **58** |
| 下單 遊戲30分鐘 | −30（保留） | **28** |
| 審核通過 整理書桌 | +10 | **38** |
| 「我拿到了」完成訂單 | ±0 | **38** |

下單確認頁（58→28）、兌換審核 sheet（兌換前58→兌換後28）、我的頁剩餘星光（38）三處數字一致，帳目正確。取消訂單（P6）退回亦即時（28→58）。

---

## 四、缺陷清單（Section 6 格式）

### DEFECT-R2-01（殘留，未修）
- **標題**：扣點 clamp 成功提示顯示「請求值」而非「實際扣除值」（Run 01 U6 未修）
- **嚴重度**：🟡 中（帳目認知誤導，非功能阻斷）
- **重現**：家長 → 設定 → 對小孩「扣點-」→ 輸入大於餘額之值（如 999）→ 送出
- **實際**：成功提示顯示「**-999 ★ → 小宇**」（請求值）
- **預期**：顯示實際扣除量（clamp 後的真實 delta，例如餘額 40 時應顯示 -40）
- **備註**：後端已正確 clamp（有「扣點後餘額不會低於 0」提示且無報錯），純提示文案問題。不在 P1–P9 範圍，建議併入下一輪。

### 觀察（非缺陷，待你確認）
- **INVITE-ENTRY**：家長端「家庭與權限」未見「邀請／新增小孩」入口，僅「移出家庭」。若邀請流程存在於別處（如小孩註冊端輸入家庭碼），請告知；若尚未實作，這會直接擋住 P9（對受邀小孩發點）的真實情境。
- **重複任務殘影**：任務列表「整理房間 ★30」卡片仍帶 ●●●（多 instance 指示），疑似 Run 01 遺留；本輪 P2 已證新建/編輯不再產生重複，此為舊資料，建議清理種子。

---

## 五、待你補驗（我因護欄無法代做）

1. **P9 受邀小孩發點**：用真正的邀請流程加入一個受邀綁定小孩 → 對其發點，確認不再報「不是家庭成員」（Run 01 U2 的核心）。
2. **A9–A11 完整註冊**：實際送出一次註冊建立新家庭（我只驗到表單＋空欄 disable，未送出）。
3. **C4「退回 3 次→錯過」** 與 **⑨ 通知 >20**：需大量來回／事件量，建議用種子腳本或人工快速製造後再看門檻行為。

---

## 六、UX 補充（併入 ux-recommendations 測試員補充區）

- 本輪**實測證實已修**：U1（我拿到了死鎖）、U3/U4（載入與在途回饋，見建立/發點的處理中狀態）、U5（連點重複，④ 已修）、U7（商城鎖說明，P5 已修）、UX-02（指派三症狀，P2 已修）、UX-03（退點空窗，P6 文案已修）、UX-04（通知不即時，P3 已修）、UX-06（登入回饋，P7 已修）、UX-08（我的頁假資料，P4 已修）。
- **仍在**：U6（扣點提示顯示請求值，見 DEFECT-R2-01）。

---

**總結**：`fix/cowork-round1` 的修復品質良好，P1–P8 全數通過，核心閉環順暢、帳目正確。剩下 P9 受邀情境與少數高成本補測項需你這端用真流程補一次；另有 U6 提示文案殘留待修。
